"use client";

/** Fixed film-grain texture across the whole landing page. pointer-events: none. */
export default function GrainOverlay() {
  return <div className="sa-grain" aria-hidden="true" />;
}
